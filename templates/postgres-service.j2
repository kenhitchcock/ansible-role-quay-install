apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: {{QUAY_NAMESPACE}}
  labels:
    app: postgres
spec:
  type: NodePort
  ports:
  - port: {{POSTGRES_PORT}}
    selector:
      app: postgres
